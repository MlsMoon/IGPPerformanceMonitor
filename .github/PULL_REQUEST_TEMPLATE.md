## Summary

-

## Test plan

- [ ] `python -m src.tests` (offscreen)
- [ ] Headless capture if metrics / CSV / sampler changed (`Scripts\capture_debug.bat`)
- [ ] i18n keys added in both `en.py` and `zh_CN.py` when UI text changed
- [ ] No secrets, credentials, or object-storage upload scripts

## Notes

-
