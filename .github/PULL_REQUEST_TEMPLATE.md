## Summary

-

## Test plan

- [ ] `python Scripts/check.py`
- [ ] `python -m src.main -t ui` (open `temp/selfcheck/shots/`)
- [ ] `python -m src.main -t update`
- [ ] `python -m src.tests` if i18n / CSV schema / manifest changed
- [ ] Capture self-check if metrics / CSV / sampler changed (`Scripts\selfcheck.bat capture`)
- [ ] i18n keys added in both `en.py` and `zh_CN.py` when UI text changed
- [ ] No secrets, credentials, or object-storage upload scripts

## Notes

-
