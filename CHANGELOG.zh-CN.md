# 更新记录

IGP Performance Monitor 面向用户的版本说明。英文原文见
[CHANGELOG.md](CHANGELOG.md)。

「帮助 → 更新记录」按界面语言选择本文件或英文文件。GitHub Release 正文由
**两份文件一起**生成（`Scripts/extract_release_notes.py`），不要只靠自动生成的
`Full Changelog: vA...vB` 对比链接交差。

二级标题必须保持 `## X.Y.Z`（与英文文件同一组版本号），应用内对话框按这些行切分。
按需要使用 `### 新增` / `### 变更` / `### 修复` / `### 面向贡献者`。

## 0.1.6

发布于 2026-09-20。

**不要下载 0.1.5。** 第一次启动的语言对话框，或「设置 → 语言」，选完后主窗口不会出现。本 0.1.6 **替换**了先前上传的 0.1.6：那一包点确定后仍会退出。如果「关于」已经显示 0.1.6、选语言还是关掉，请重新下载本包；「检查更新」看不到更高的版本号。

### 修复

- 第一次启动的 **Language / 语言** 和「**设置 → 语言**」不会再把应用关掉。0.1.5 会把关掉选择框当成「最后一个窗口关了」，Qt 在主窗口 `show()` 之前就 `quit()`。先前那份 0.1.6 点确定后仍会退出：选中的语言没有存上，进程直接结束，窗口出不来。请到 GitHub Releases 重新下载
  （https://github.com/MlsMoon/IGPPerformanceMonitor/releases/tag/v0.1.6），即使已经装过 0.1.6。

### 面向贡献者

- `-t startup` 会拉起真正的 `python -m src.main`（有打包 EXE 时也会测），走第一次启动选语言的路径。`-t ui` 进不了 `main()`。

## 0.1.5

发布于 2026-09-20。

**有严重问题，不要下载 0.1.5。** 第一次启动的语言对话框和「设置 → 语言」选完后主窗口不会出现。请下载 [0.1.6](https://github.com/MlsMoon/IGPPerformanceMonitor/releases/tag/v0.1.6)。

菜单栏加了「设置」和界面语言切换。「帮助 → 检查更新」连 GitHub 时不再把窗口冻住，第一次启动会先问用哪种语言。

### 新增

- 「**设置**」菜单里放深色模式、窗口总在最前、鼠标穿透和 **语言**。「查看」只负责显示（图表、图表面板、显示/隐藏叠加窗）。文件 / 查看 / 设置 / 帮助在菜单栏各有一个 16×16 的小图标。
- 第一次启动、还没有保存过语言时，主窗口出现前会弹出 **Language / 语言** 对话框，选 English 或简体中文。选择写进 `%APPDATA%\IGPPerformanceMonitor\config.json`。之后用「**设置 → 语言**」可在同一次运行里切换，不用再走 UAC。若设置了 `IGP_LANG`，仍以环境变量为准。

### 变更

- 「帮助 → 检查更新」和启动时的静默检查改在工作线程里拉取 `app_manifest.json`。以前窗口会按 GitHub 的超时冻住最多约 20 秒；现在状态栏显示「正在检查更新…」，这两个帮助项在出结果前保持禁用。下载/安装本来就已经走进度对话框。
- 深色模式、窗口总在最前、鼠标穿透从「查看」挪到「设置」。叠加窗开了鼠标穿透、点不到右键菜单时，改从「设置」菜单关，而不是「查看」。

### 面向贡献者

- 界面文案在 `src/i18n/locales/<locale>.json`（`native_name` + `strings`），不再写在 Python 字典里。`tr()` 不变。PyInstaller 会把该目录打进包。

## 0.1.4

发布于 2026-09-18。

采集能区分同一程序的两个副本。应用内手册补上了真实窗口截图，「帮助 → 更新记录」
跟界面语言走，导出和「查看」菜单上的两处现场问题也修好了。

### 新增

- 「进程选择」把同一个 exe 的每个正在运行的副本单独成行：`名称  (pid)  —  窗口标题`。两个开着不同工程的 Unity 编辑器不再被合成一个目标。单击一行会闪一下对应窗口；右键「切换到此窗口」。采集按进程 id 挂接。监视列表按 exe + 标题保存，重启后按标题重新绑定。
- 「帮助 → 用户手册」收录真实窗口照片（进程面板、主界面、叠加窗、CSV 对话框、浅色主题），英简中各一套。插图按面板宽度缩放。文本选区改成灰色罩层，不再用 Qt 默认的紫/蓝高亮。

### 变更

- 「帮助 → 更新记录」按界面语言打开对应文件（`CHANGELOG.md` 或 `CHANGELOG.zh-CN.md`），两边版本列表相同。GitHub Release 正文由这两份文件生成，不再是 0.1.0–0.1.3 那种只有 `Full Changelog: vA...vB` 对比链接的空说明。
- 「文件 → 导入 CSV」/ 离线分析按实例（exe + PID）分组画曲线，同一个文件里两个 `Unity.exe` 会变成两条线，而不是混成一条。

### 修复

- 「文件 → 导出 CSV」会提醒：导出的是当前这次采集。再次开始采集会清空已采集的帧，之后再导出不会带上本次内容。对话框本来就有帧数；没有这句说明时，很容易以为上次的数据还在文件里。
- 「查看」菜单的文字现在左对齐。以前 **图表**（子菜单）和 **叠加窗**（普通项）会比可勾选项（深色模式、总在最前、点击穿透）更靠左，因为 Qt 5.15.2 的样式表风格只给可勾选行使缩进。
- 「查看 → 深色模式」关掉后，实时统计表还会留在深色卡片上。重建表格时只把控件从布局里拿掉、没有取消父级，旧的深色框就还在画面上。

### 面向贡献者

- `python -m src.selfcheck.docs_shots` 负责拍摄、评判，并把用户手册 PNG 拷进 `docs/images/`。
- UI 自检还会抓打开的「查看」菜单（`dark-view-menu.png` / `light-view-menu.png`），缩进回退一眼能看出来；主题切换后如果还有旧的 `StatsList` 挂在树上也会报错。没有采集数据的空会话（CI 就是这样）只显示占位符，不算错误。
- GitHub Release 正文由 `Scripts/extract_release_notes.py` 写出；`release.yml` 设置了 `generate_release_notes: false`。

## 0.1.3

发布于 2026-09-18。

系统信息改成真正的可折叠区块，源码运行会标明开发模式，Qt 的 libpng 警告也不再刷控制台。

### 新增

- 从源码（未打包）运行时，窗口标题带 **[dev]** 标记，状态栏有常驻开发徽章，一眼能看出当前是开发会话。打包后的 EXE 两种都不会显示。

### 变更

- 实时 CPU、GPU、内存、显示器读数改为同一可折叠区块里的键值行。折叠时只收起内容行，标题不再一起消失。
- 「帮助 → 用户手册」改为标题树（页面加上二、三级标题），链接颜色跟主题走，不再是扁平列表和 Qt 默认的蓝色链接。

### 修复

- Qt 自带图标会通过 C 的 stderr 打印 libpng iCCP 警告。这些行不再出现在控制台（包一层 Python 的 `sys.stderr` 拦不住）。

### 面向贡献者

- 验证改为 `Scripts/check.py`，加上规划器按 `AREA.touches` 选出的 `-t` 自检，而不再维护大型断言套件。
- 未指定版本号的 `release` 默认只升补丁号（`X.Y.Z` → `X.Y.(Z+1)`），不再根据提交列表自行推断次版本或主版本。

## 0.1.2

发布于 2026-09-17。

用户手册打进应用。停止采集时叠加窗会收起。更新下载不再把整个窗口冻住。

### 新增

- 「帮助 → 用户手册」按当前界面语言打开随包指南（`docs/en` 或 `docs/zh-CN`）。同一套 `docs/` 会打进便携版 EXE，安装器也会拷到 EXE 旁边。繁体中文和日文副本仍在磁盘上，并从上述页面互相链接。
- 「检查更新」和回退在工作线程里跑，前面是进度对话框：字节数与百分比；服务器没给 `Content-Length` 时用不确定进度条；SHA256 校验也有进度（给约 55 MB 算哈希看起来会像卡死）；可以取消。取消后会等文件句柄关掉再删掉半截文件，这样 Windows 才能删成功。

### 修复

- 停止采集时会关掉叠加窗。跟随定时器以前每 33 ms 调一次 `show()`，所以点停止、自动停止、或目标进程退出后，屏幕上还会冻着最后一帧 FPS。
- 系统信息条里过长的 GPU / 显示器名称不再裁字，也不会把窗口撑宽。芯片高度按标签计算（样式表 padding 算进内容边距，字体度量不含这一截）；宽度留出几个像素余量，避免最后一个字形被切掉。

## 0.1.1

发布于 2026-09-17。

扁平视觉改版：更密的界面、深色原生标题栏、更少动效，以及真正透明的图标。

### 变更

- 更扁平：去掉阴影和表面描边，圆角上限 6 px。原来堆叠的十块统计磁贴改成一张用细线分隔行的表。
- Windows 上使用深色原生标题栏，深色主题不再在窗口上方留一条白边。
- 动效只保留微交互：没有启动渐显，悬停反馈 90 ms，退出是进入时长的 75%，并且绝不动画布局属性（信息条高度动画以前每一帧都会重排图表）。
- 侧栏、进程列表、统计/图表分割条占满窗口高度。过长的进程名和 GPU 名改为省略号，不再把窗口最小宽度撑过 1100 px。
- 重绘应用图标：圆角砖块外侧改为真正透明（以前是不透明白底，在深色桌面/任务栏和 GitHub 深色 README 上会框出一圈白边），圆角更紧，16–32 px 使用简化的三杠变体（原来的趋势线在这个尺寸看不清）。

### 面向贡献者

- UI 自检不再把主题、图表显隐或分割条状态写进开发者自己的 `%APPDATA%`。

## 0.1.0

发布于 2026-09-17。

首次公开发布。Windows 桌面应用，封装 Intel PresentMon 2.4.1，为每一帧补上系统与进程指标，并绘制实时图表。

### 新增

- Windows 安装包（`IGPPerformanceMonitor-Setup-0.1.0.exe`）和单文件便携版 EXE。两者都需要管理员权限（PresentMon 使用 ETW）。
- 应用内从 GitHub Releases 更新：`app_manifest.json`，SHA256 校验便携版 EXE 与更新器，并可回退到上一份已发布版本。
- 通过 Intel PresentMon 2.4.1 实时监视多个应用：FPS、帧时间、CPU、GPU、内存、显存，以及跟随目标窗口的置顶叠加窗。
- CSV 导出/导入，含离线卡顿分析。
- 深色/浅色主题。界面为英语和简体中文（`IGP_LANG=en` 或 `zh_CN`）。

### 面向贡献者

- 项目技能、源码注释和贡献者文档为英文。界面文案在 `src/i18n/zh_CN.py`。
